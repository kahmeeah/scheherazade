export default function Scene_03({ next }) {
  return (
    <section className="scene">
      <div>How it was late, and no one could sleep,</div>
      <div>the horses running</div>
      <div>until they forget that they are horses</div>
      <button onClick={next}>Next Scene</button>
    </section>
  );
}